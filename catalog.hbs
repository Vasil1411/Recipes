 <h2>Recipe Catalog</h2>
        {{#if recipes}}
        <section class="recipes">
           {{#each recipes}}
                {{> recipe}}
                {{/each}}
                {{else}}
                <p class="no-post">There are no recipes found yet!</p> 
                {{/if}}
        </section>
   